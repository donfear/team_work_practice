language: node_js
node_js:
- '14'
before_script:
- sh prepare-deploy.sh
script:
- npm run build
deploy:
  provider: pages
  keep_history: true
  skip_cleanup: true
  github_token: $github_token
  on:
    branch: main
